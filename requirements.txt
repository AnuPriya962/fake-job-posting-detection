streamlit>=1.28.0       # Builds the interactive web application and user interface dashboard
scikit-learn>=1.3.0    # Provides machine learning models, TF-IDF vectorization, and evaluation metrics
pandas>=2.0.0          # Handles dataset loading, tabular data manipulation, and structured feature parsing
numpy>=1.24.0           # Performs numerical operations, array processing, and matrix transformations
joblib>=1.3.0          # Serializes and loads trained model artifacts (.joblib) without retraining
matplotlib>=3.7.0      # Renders model evaluation charts, feature importance plots, and confusion matrices
seaborn>=0.12.0        # Enhances data visualizations with styled heatmaps and performance distribution plots
pytest>=7.4.0         # Runs automated unit tests for data ingestion, preprocessing, and prediction logic
